import { OnModuleInit } from '@nestjs/common';
import { Repository } from 'typeorm';
import { User } from "../../databases/entities/user.entity";
import { Student } from "../../databases/entities/student.entity";
import { Parent } from "../../databases/entities/parent.entity";
import { TestResult } from "../../databases/entities/test-result.entity";
import { Attendance } from "../../databases/entities/attendance.entity";
export declare class TelegramService implements OnModuleInit {
    private readonly userRepo;
    private readonly studentRepo;
    private readonly parentRepo;
    private readonly resultRepo;
    private readonly attRepo;
    private bot;
    private broadcastWriting;
    private pendingBroadcasts;
    constructor(userRepo: Repository<User>, studentRepo: Repository<Student>, parentRepo: Repository<Parent>, resultRepo: Repository<TestResult>, attRepo: Repository<Attendance>);
    onModuleInit(): void;
    private normalizePhone;
    private getCurrentUser;
    private getRoleLabel;
    private canBroadcast;
    private showWelcome;
    private linkPhoneToUser;
    private showMainMenu;
    private showChildrenResults;
    private showStudentResults;
    private showStudentAttendance;
    private showChildrenStats;
    private showChildrenAttendance;
    private showSupportRights;
    private showSupportStudentStats;
    private showTeacherGroupStats;
    private showTeacherGroupAttendance;
    private showAdminContact;
    private startBroadcast;
    private askConfirmBroadcast;
    private executeBroadcast;
    private setupCommands;
    broadcastFromAdmin(message: string): Promise<void>;
    notifyAdmins(message: string): Promise<void>;
    sendNotification(telegramId: string, message: string): Promise<void>;
    notifyAttendance(studentId: number, isPresent: boolean): Promise<void>;
}
