export enum TestStatus {
  ACTIVE = 'ACTIVE',
  IN_PROGRESS = 'IN_PROGRESS',
  NOACTIVE = 'NOACTIVE',

  // legacy values — eski testlarda bo'lishi mumkin
  PAUSED = 'PAUSED',
  FINISHED = 'FINISHED',
}
